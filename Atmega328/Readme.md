# **Discontinued Firmware**

See `firmware` folder in the root of the emonPi repo for latest firmware. This `Atmega328` folder has been kept here to avoid breaking older emonpi's that might be attemping to pull update firmware from this folder
